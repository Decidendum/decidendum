
/**
 * 
 */

;(function(window, document, $, undefined) {
  'use strict';

  // var $header = $('#header');

  // $(window).on('scroll', function(e) {

  //   $header.css('background-position-y', (100 + (window.scrollY * 100 / window.innerHeight)) + '%');

  // });

})(window, document, jQuery);
